<h1><%= @list.name %></h1>

    <p><%= @list.name %></p>
<br>

    <span class="mb-2 d-block"><small><em class="text-muted"><%= "You haven't saved any movies yet!" if @list.movies.empty? %></em></small></span>

  <h2>All movies in <%= @list.name %></h2>
    <% @list.movies.each do |movie| %>
      <% bookmark = Bookmark.find_by(list: @list, movie: movie) %>
      <%= image_tag movie.poster_url, alt: movie.title %>
      <%= movie.title %> <span> <%= movie.rating %></span>
      <%= movie.overview %>
      <%= bookmark.comment %>
      <%= link_to "delete a movie", bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to remove #{movie.title} from your #{@list.name} list"}, class: 'text-danger' %>
    <% end %>

  <%= link_to 'Add a Movie', new_list_bookmark_path(@list), class: 'btn btn-primary' %>
      <%# <%= link_to "delete this list", list_path(@list), class: "btn btn-warning", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this list?" } %>
    <%= link_to "delete this list", new_list_path(@list), method: :delete, data: { confirm: "Are you sure you want to delete this list?" }, class: "btn btn-warning" %>




<div><%= link_to "Back to lists", "/lists", class: "btn btn-primary"%>
</div>
